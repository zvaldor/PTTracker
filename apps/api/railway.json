{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "sh -c 'echo \"Starting API...\" && echo \"DATABASE_URL: ${DATABASE_URL:0:20}...\" && npx prisma migrate deploy && echo \"Migrations complete!\" && node dist/main'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
